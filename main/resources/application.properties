spring.application.name=chatllm

# Server
server.port=8080

# Datasource (Postgres)
spring.datasource.url=jdbc:postgresql://localhost:5432/llm_db
spring.datasource.username=llm_user
spring.datasource.password=llm_user

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT (????????? - ?????? ?? ?????? secret ?? prod)
app.jwt.secret=mySecretKeyThatIsAtLeast32CharactersLongForHMAC256AlgorithmSecurity123
app.jwt.expiration-ms=3600000

# Model & Embedding endpoints (?? ?? ???????????????? ??? ModelGatewayService ??? EmbeddingService)
app.model.endpoint=http://localhost:8082/v1/generate
app.embedding.endpoint=http://localhost:8083/v1/embed

